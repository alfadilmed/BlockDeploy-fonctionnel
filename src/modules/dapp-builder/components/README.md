# Draggable & Droppable Components

This directory will contain the actual "Drag & Drop" components that users can add to their dApps.
This includes both standard UI components (Text, Button, Image) and Web3-specific components (WalletConnect, ContractDataReader, etc.).
